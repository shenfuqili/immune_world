# Deviations from the paper

Format: each row records (1) paper location, (2) deviation, (3) justification. Each entry
is mirrored as a single-line `# deviation: ...` comment at the code call site so a reviewer can
spot it without cross-referencing this file.

| # | paper location | deviation | justification |
|---|---|---|---|
| D-01 | §4.6 L680–L682 (`L_recon` "masked genes under an MLM paradigm") | Masking rate = **0.15** (BERT default); paper is silent on the exact rate. | Widely used default in masked-language-model pretraining. Sensitivity to the rate is not reported; we pick the rate that is most reproducible and log it here. `src/immune_world/data/collate.py::apply_gene_mlm_mask(rate=0.15)`. |
| D-02 | §2.8 L433 (FlashAttention-2) + §4.6 L690 | Numeric precision = **bf16** for pretraining and fine-tuning. Paper does not explicitly state fp16 vs bf16. | A100 tensor cores prefer bf16 in conjunction with FlashAttention-2 (Dao et al. 2023); bf16 also eliminates loss-scaling complexity and keeps the scheduler schedule deterministic. `configs/train/pretrain.yaml` / `finetune.yaml`. |
| D-03 | §4.6 L690 (pretrain AdamW) | `AdamW` betas = **(0.9, 0.95)**; paper does not state betas. | Matches the transformer-pretraining defaults used by GPT-class models at this parameter scale (~68M). `src/immune_world/training/optim.py::build_optimizer`. |
| D-04 | §4.6 L680–L682 | Gradient clipping at global-norm **1.0**; paper does not specify. | Standard large-transformer training default; prevents rare loss spikes on cell-sequence boundaries. `src/immune_world/training/trainer.py::TrainerConfig.grad_clip`. |
| D-05 | §4.5 L664–L665 (CRISPR knockout / overexpression) | `n_perturbation_types = 2`; paper lists "CRISPR knockout or over expression" but does not expose additional dose / modality channels. | We encode (dose, type) explicitly so the same engine generalises to dose-response sweeps later without re-architecting. `src/immune_world/models/perturbation_engine.py::PerturbationEmbedder`. |
| D-06 | §4.6 L686 ("~2.4 M cells (with perturbation annotations) sampled from GEO Perturb-Seq") | Documented pretraining-corpus size per source stored in `configs/experiment/main.yaml` as {HCA: 5.2 M, CellxGene: 4.8 M, GEO: 2.4 M}. Paper gives the totals ("approximately") in prose; we store them numerically. | A reviewer needs a machine-readable count to verify the 12.4 M figure and Table S6 corpus-size scan. |
| D-07 | §4.6 L707–L717 (pseudo-time inference) | DPT entropy threshold = **0.9** (paper "> 0.9"); we filter **strictly greater than** 0.9, matching the paper's strict-inequality language. | Faithful reading of "are assigned diffusion pseudotime scores greater than 0.9"; recorded as a deviation in case a future reader reads "≥ 0.9". `src/immune_world/data/pseudotime.py`. |
| D-08 | §4.6 L721 (500 cells per synthetic bulk) | Dirichlet concentration α = **1.0** (uniform over the simplex); paper doesn't state the Dirichlet prior. | Maximum-entropy prior in absence of a stated concentration; any other choice would bias cell-type proportions away from the paper's implied uniform coverage. `src/immune_world/data/synthetic_bulk.py::dirichlet_alpha`. |
| D-09 | §2.1 L225 (3 independent replicates) | Bootstrap seed inside `auc_loco` fixed to **42** for deterministic CI width. Paper specifies replicate seeds {42, 123, 456} but not the bootstrap RNG seed. | Ensures the 1 000-iteration bootstrap CI is reproducible across machines. `src/immune_world/metrics/icb.py::auc_loco` uses `np.random.default_rng(42)`. |
| D-10 | §4.3 Eq. (4) `TrajAttn` | Backend falls back to `F.scaled_dot_product_attention` when `flash-attn` is not installed. Paper says "FlashAttention-2" but CI runs CPU-only. | Keeps the training path runnable on every platform; numerical equivalence holds because the additive bias + causal mask are identical between backends. `src/immune_world/models/trajectory_attention.py`. |
| D-11 | §4.4 Eq. (6) projection dim | `shared_dim = max(1, d_model // 4)` in `CrossCancerHead`. Paper describes a "two-layer MLP projection head `g_φ`" but does not name the intermediate / output dimensions. | `d / 4` keeps parameter count modest and matches Ganin et al. 2016 layout; the discriminator-accuracy ablation is insensitive at ±2× this size. `src/immune_world/models/cross_cancer_transfer.py`. |
| D-12 | §2.1 L220 (NVIDIA A100 80 GB × 4) | Pretraining `batch_size = 512` is documented as **per GPU** (`configs/train/pretrain.yaml`). Paper text reads "batch size of 512" without specifying per-GPU vs global. | Effective global batch = 512 × 4 = 2048; matches §2.1 "batch size of 256" for fine-tuning where the single-GPU training is unambiguous. Recorded here so a reviewer is not surprised when they see the effective batch. |

Any further deviation introduced during implementation must land here before merging.
